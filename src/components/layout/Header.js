import React, { Component } from 'react';
import { NavBar } from './TrucNavBar';

export default class Header extends Component {
  render() {
    return (
      <header>
        <NavBar />
      </header>
  )}
}
